# AGENTS.md

Agent-agnostic personal instructions. This file is the canonical source;
`~/.claude/CLAUDE.md` is a symlink to it, so Claude Code loads it as
user-level memory across all projects.

## Writing

- Never use the em dash "—". Use a plain dash "-" instead.

## Git

- When writing commit messages, NEVER auto-add your agent name as co-author.
